# AGENTS.md — grok-build-ager

Translator. Author graphs with `okf-agent-graph`.

- `/ager-to-grok-build`
- `python3 scripts/emit.py --bundle path/to/ager --out ./generated/grok-build`

Isolation: do not write a shared second brain `main` without an AGER session.
