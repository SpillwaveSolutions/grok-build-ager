---
name: grok-bot-grok-build-ager
description: Bind a Grok Bot agent to grok-build-ager.
---

# Grok Bot / grok-build-ager

1. Identity: `grok-bot/grok-build-ager`
2. Grok Build loads the Claude plugin layout with zero config.
3. Run `scripts/emit.py`.
