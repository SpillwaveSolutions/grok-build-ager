# Grok Build mapping

- Plugin name = AGER graph id
- Identity pin lives in `.grok-plugin/marketplace.json` (`claude_plugin: true`, `zero_config: true`)
- Skills use the Agent Skills layout (`skills/*/SKILL.md`) Grok Build already loads
- `AGENTS.md` states LoopPolicy numbers so a Grok session does not invent extra agents
- Price budget is documented, not metered by the host
