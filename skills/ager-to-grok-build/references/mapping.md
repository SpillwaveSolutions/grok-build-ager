# Grok Build mapping

- Plugin name = AGER graph id
- Identity pin in `.grok-plugin/marketplace.json` (`claude_plugin: true`, `zero_config: true`)
- Skills use Agent Skills layout Grok Build already loads
- Price budget is documented, not metered
