# grok-build-ager

AGER → **Grok Build** plugin. Claude-compatible layout plus `.grok-plugin` identity pin.

```bash
ager-compile --target grok-build --out ./generated
```

Compiler: [ager-translators](https://github.com/SpillwaveSolutions/ager-translators).
